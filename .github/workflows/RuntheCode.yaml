name: Run the Code

on: [push,pull_request]

jobs:
    run-the-code:
      runs-on: ubuntu-latest
      steps:
        - name: Install all the dependencies
          run: npm i
        - name: Build the code
          run: num run build
        - name: Start the code
          run: npm start  